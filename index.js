// add solution here

var musiciansarray = [];
var instrumentsarray = [];
var theBeatlesarray = [];

function theBeatlesPlay(musicians, instruments) {
  
}

for (i = -1, i < musiciansarray.length, i++) {
  return `${musiciansarray[i]} plays ${instrumentsarray[i]}`;
}